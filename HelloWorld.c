#include <stdio.h>//includes the standard input/output library
int main(){//Its main function, serves as the entry point of the program
    printf("Hello World!");// function to print the text (The text is enclosed in double quotes, and it will be displayed as it is)
    return 0;//a return value of 0 from the main function conventionally indicates that the program has executed without errors
}